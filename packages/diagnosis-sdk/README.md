# Diagnosis SDK

Diagnosis 资源包用于承载课堂即时诊断、错因标签、反馈策略和补救建议。它不等同于普通练习题库，核心目标是帮助教师快速判断学生在哪个数学理解环节发生偏差。

首期只定义内容包规范和 metadata schema，不实现诊断播放器。

## 目录

- `schemas/diagnosis-metadata.schema.json`：Diagnosis metadata schema。
- `docs/resource-package.md`：Diagnosis 资源包结构、状态和进入平台的门槛。

