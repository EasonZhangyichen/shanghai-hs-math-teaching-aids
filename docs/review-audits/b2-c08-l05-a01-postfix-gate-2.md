# B2-C08-L05-A01 修复后质量闸门复核 2

- 复核日期：2026-05-18
- 复核分支：`codex/review-b2-c08-l05-a01-postfix-gate-2`
- 复核对象：`SH-HS-MATH-HJ-B2-C08-L05-A01`「数量积动态解释器」
- 写入范围：仅本报告；未修改资源包、平台源码、课程图谱、backlog、generated files 或 Manim 产物

## 总体结论

`SH-HS-MATH-HJ-B2-C08-L05-A01` 本轮通过修复后数学口径复核：数量积定义、有向投影、零向量边界、锐角/直角/钝角符号判断均已收紧，未发现把零向量误读成普通垂直或普通投影为 0 的残留。资源内也未外显 8.3 的坐标表示公式训练。

浏览器侧，直达页在 `1366x768`、`1280x720`、`1024x768` 下核心图像、读数、符号解释、按钮和拖拽端点均首屏可用；平台页滚动到资源卡后，iframe 内部首屏可用。平台外层仍需滚动到该资源卡，尤其 `1024x768` 下资源位置较深，因此本轮不建议直接进入 `classroom_trial`。

建议状态：`math_review_ready_and_teacher_trial_prep_ready_with_outer_scroll_note`。可进入教师限时试读准备，但应以“直达页优先、平台页需预先滚动定位”为试读说明；不得进入 `classroom_trial`、`release_candidate` 或 `published`。

## 依据

- 项目规则：`docs/00-project-brief.md`、`docs/01-current-state.md`、`docs/02-next-actions.md`、`docs/git-workflow.md`、`docs/content-standards.md`、`docs/codex-collaboration-guide.md`、`docs/parallel-quality-system.md`、`docs/resource-factory-workflow.md`
- 课程图谱：`content/curriculum/index.yaml` 中 B2-C08-L05 对应 8.2.2「向量的数量积的定义与运算律」
- 资源只读检查：`content/applets/SH-HS-MATH-HJ-B2-C08-L05-A01/**`
- 前置复核：`docs/review-audits/b2-c08-l05-a01-postfix-gate.md`、`docs/browser-audits/b2-c08-l05-a01-touch-gate.md`

## 数学口径复核

| 检查项 | 结论 | 证据 |
| --- | --- | --- |
| 数量积定义 | 通过 | 资源使用 `a·b = |a||b|cosθ`，并将数量积解释为数量而不是向量。 |
| 投影为 0 | 通过 | `90°` 场景将 `cosθ = 0`、投影为 0、`a·b = 0` 与直角联系起来；同时说明该垂直判断只适用于两个非零向量。 |
| 负投影 | 通过 | `120°` 场景显示 `θ = 120° = 2π/3`、`cosθ = -0.50`、投影 `-2`、`a·b = -6`，解释为投影段落在 `b` 的反方向上。 |
| 锐角/直角/钝角 | 通过 | 页面和教师脚本均以 `cosθ` 正、零、负连接 `a·b` 正、零、负，课堂口径清楚。 |
| `a` 为零向量 | 通过 | 显示“a 为零向量，投影 = 0”，并提示这不是两个非零向量垂直。 |
| `b` 为零向量 | 通过 | 显示“投影方向未规定”，并提示不能说 `a` 在 `b` 方向上的投影等于 0。 |

## 8.3 坐标前置复核

未发现把 8.3「向量的坐标表示」提前引入 8.2.2 的实质问题：

- 未外显 `a = (x1, y1)`、`b = (x2, y2)`、`a·b = x1x2 + y1y2` 等坐标公式训练。
- 页面保留淡网格和原点 `O`，但资源文档已说明这是拖拽与观察用背景，不作为本课公式来源。
- 代码内部使用坐标计算画面与读数，不等同于课堂外显的坐标表示教学。

注意：课程图谱中第 8 章仍有少量英文符号 fallback，例如 `cos theta`。本轮禁止修改 `content/curriculum/index.yaml`，且目标资源包可见文本未发现该问题；建议总控在后续全局符号清理中单独处理。

## 直达页与平台 iframe 复核

测试方式：

- 直达页：`/content/applets/SH-HS-MATH-HJ-B2-C08-L05-A01/src/index.html`
- 平台页：`/#lesson=SH-HS-MATH-HJ-B2-C08-L05`
- 视口：`1366x768`、`1280x720`、`1024x768`
- 工具：Codex Browser，本地 Vite 预览，iframe frame locator 与 DOM 度量

| 场景 | 结论 | 证据 |
| --- | --- | --- |
| 直达页首屏 | 通过 | 三个视口下页面无需滚动；核心图像、公式、夹角/余弦/投影/数量积读数、符号解释、`下一步`、`重置`、典型角按钮均在首屏。 |
| 直达页交互 | 通过 | 点击 `120°` 后读数为负投影与负数量积；点击 `重置` 后回到 `60°` 正数量积状态。 |
| 平台 iframe 内部 | 通过 | 滚动到资源卡后，iframe 尺寸约为 `950x560`、`864x560`、`936x560`；内部读数、符号解释、按钮和拖拽端点可用。 |
| 平台外层首屏 | 带风险通过 | 资源卡不在平台页初始首屏内，需教师预先滚动定位；这不影响 iframe 内部首屏，但影响真实课堂节奏。 |
| 滑块 | 不适用 | 该资源没有 `input[type="range"]` 滑块，交互由典型角按钮和向量端点拖拽完成；两类实际控件均可用。 |
| 控制台 | 通过 | 未发现目标资源脚本错误；平台既有 iframe sandbox warning 不属于本资源缺陷。 |

## 符号表达复核

目标资源的课堂可见文本使用 `θ`、`π`、`a·b`、`cosθ`、`|a|`、`|b|` 等中文课堂可接受表达，未发现 `theta`、`pi`、`omega`、`phi` 等 fallback 作为学生可见数学符号出现。

实现源码中存在 JavaScript 变量名和 SVG 属性等英文标识，属于代码内部实现，不进入课堂展示文本。若后续做全章符号清理，仍建议把课程图谱中的英文 fallback 与资源包可见文本分开处理，避免把只读图谱问题误判为本资源回归。

## 教师限时试读准备判断

教师脚本以 10 到 12 分钟组织“观察夹角 -> 读投影 -> 对照典型角 -> 辨析零向量边界”，学生任务只要求完成符号表、解释 120° 负投影并口头辨析零向量。任务量适合进入教师限时试读准备。

但本资源仍不应直接进入 `classroom_trial`：

- 平台外层需要预先滚动到资源卡，真实课堂中会消耗时间。
- 真实触控屏、投屏缩放、低配教师机和 Safari 仍未完成一线设备复核。
- B2-C08 全章仍有 `needs_manual_textbook_check` 风险，8.3 边界仍需总控终核。

## 建议状态与剩余风险

- 数学闸门：通过，可建议进入 `math_review` 链路。
- 浏览器闸门：资源内通过；平台外层保留滚动定位风险。
- 课堂闸门：可进入教师限时试读准备；不得直接进入 `classroom_trial`。
- 发布链路：不得进入 `release_candidate` 或 `published`。

剩余风险：

1. 平台课时页初始首屏离目标 iframe 较远，尤其 `1024x768` 下需要较多滚动。
2. 真机触控屏、投屏缩放和不同浏览器还需一线试读前复核。
3. 第 8 章课程图谱仍需纸质教材或已登录平台终核，且图谱中少量英文符号 fallback 需总控统一清理。

