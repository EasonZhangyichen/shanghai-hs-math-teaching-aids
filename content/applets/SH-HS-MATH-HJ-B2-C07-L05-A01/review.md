# 审核记录：三角函数参数变化实验室

资源 ID：`SH-HS-MATH-HJ-B2-C07-L05-A01`
版本：`0.1.0`
当前状态：`draft`
最后更新：2026-05-01

## 草稿自检结论

该资源包已创建 metadata、README、教师脚本、学生活动、审核记录和 `src/index.html` 可运行原型。当前仍保持 `draft`，用于课堂脚本试读、数学边界复核和浏览器交互验证；尚未进入数学审校。

## 数学审校清单

- [x] 明确讨论对象为 `y = A sin(omega x + phi)`，默认不加入竖直平移。
- [x] 明确振幅为 `|A|`，`A < 0` 时包含关于 x 轴翻折。
- [x] 明确在 `omega > 0` 的课堂默认范围内，周期为 `2pi / omega`。
- [x] 明确相位平移量为 `-phi / omega`。
- [x] 关键点追踪基于 `omega x + phi = kpi` 和 `omega x + phi = pi/2 + kpi`。
- [ ] 数学审校者需复核 `A < 0` 时最大值点和最小值点的解释口径。
- [ ] 数学审校者需确认是否需要在本课时提前讨论 `omega < 0` 的等价处理。

## 教学审校清单

- [x] 对应沪教版必修第二册第 7 章 7.3“函数 y = A sin(omega x + phi) 的图像”。
- [x] 聚焦课程图谱中记录的三个痛点：横向伸缩和平移方向混淆、多参数认知负荷、从图像反推参数干扰。
- [x] 教师脚本包含单参数观察、变换顺序比较和反推参数流程。
- [x] 学生活动包含预测、观察、记录、归纳和快速诊断。
- [x] 明确本资源不系统展开竖直平移、余弦和正切参数函数。
- [ ] 需由一线教师确认 25 分钟课堂节奏是否适合两课时中的第一课时。

## 交互审校清单

- [x] 一个资源只聚焦“变换”这一主要认知动作。
- [x] 初始状态与 `y = sin x` 重合，支持逐步引入参数。
- [x] 支持单参数模式，避免多参数同时变化造成过高认知负荷。
- [x] 支持显示中间曲线、关键点、振幅范围和归纳面板。
- [x] HTML 原型已实现参数滑块、观察点拖动、播放、重置和图层切换。
- [ ] 需完成大屏可读性、触控拖动和普通浏览器兼容性验证。

## 版权与合规清单

- [x] 本草稿为原创脚本、任务与交互实现。
- [x] 未复制教材正文、官方 PPT、教案或商业平台资源。
- [x] 未引入外部代码、图片或第三方素材。
- [x] 当前实现使用原生 HTML/CSS/SVG/JavaScript，无第三方库 license 记录需求。

## 进入下一状态的门槛

进入 `math_review` 前至少需要：

1. 对 A、omega、phi 三个参数的数学表达做人工复核。
2. 复核相位平移量 `-phi / omega` 的课堂语言是否清晰。
3. 完成浏览器交互一致性检查，特别是关键点、周期和相位标记。
4. 由一线教师试读 25 分钟课堂节奏。

进入 `pedagogy_review` 前至少需要：

1. 数学审校无阻塞问题。
2. 教师脚本和学生活动根据试读反馈完成同步调整。
3. 大屏演示和触控操作无明显可用性问题。

## 待确认问题

- 是否在首版课堂中允许 `A = 0` 作为退化观察，还是完全避开。
- 是否把 `omega < 0` 放到拓展问题，而不是主线。
- 后续 `SH-HS-MATH-HJ-B2-C07-L05-D01` 是否直接复用本 Applet 的错因标签命名。
