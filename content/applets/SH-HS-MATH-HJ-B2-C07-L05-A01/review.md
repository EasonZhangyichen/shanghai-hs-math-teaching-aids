# 审核记录：三角函数参数变化实验室

资源 ID：`SH-HS-MATH-HJ-B2-C07-L05-A01`
版本：`0.1.0`
当前状态：`draft`
最后更新：2026-05-11

## 2026-05-11 课堂试读与首屏小修结论

本轮只修订 `SH-HS-MATH-HJ-B2-C07-L05-A01` 的课堂脚本、学生活动、README、审核记录和 Applet 首屏密度；未修改课程索引、生产 backlog、平台源码或发布状态。

修订目标来自两份审核报告：

- 课堂试读准备度报告指出原 25 分钟脚本不适合直接作为 12 到 18 分钟课堂片段，建议压缩或拆成两段。
- 浏览器与大屏触控 QA 报告指出直达页和平台 iframe 内部文档高度约 `1273px`，教师需要滚动才能看到完整控制面板、变换顺序、图层和归纳区。

本轮处理：

- 教师脚本改为“18 分钟主试读片段 + 12 到 15 分钟可选延伸片段”。主片段只保留 A、ω、φ 单参数观察和一个出口反推；变换顺序比较拆到第二片段。
- 学生活动改为轻量必做任务：A 表、ω 表、φ 表和一个出口反推；变换顺序表降为可选延伸，不再要求同一 18 分钟片段全部完成。
- README 同步课堂试读建议和分段使用方式。
- `src/index.html` 只做首屏高度、控制区密度和响应式布局小修，目标是降低 iframe 首屏滚动风险，不改变数学模型或交互协议。

本轮浏览器复测：

- Codex in-app Browser 连接连续超时，本轮记录为 Browser-path 阻塞，改用仓库外临时 Playwright 脚本 + 系统 Chrome 复测，不向仓库写入截图或临时脚本。
- 直达页 `1366 x 768` 与 `1920 x 1080` 下，`documentElement.scrollHeight` 均等于视口高度，不再由内容撑出整页滚动。
- 平台 iframe `1366 x 768` 下，iframe 内 `scrollHeight = clientHeight = 560`，A、ω、φ 三个核心滑块均在 iframe 首屏可见；变换顺序按钮仍在控制面板下方，需要控制面板内滚动。
- 平台 iframe `1920 x 1080` 下，iframe 内 `scrollHeight = clientHeight = 760`，播放、模式、A/ω/φ、变换顺序均在首屏可见。
- 交互读回：设 `ω = 2`、`φ = π/2` 后周期显示为 `π`，切换“平移后伸缩”后说明文本仍为“最终平移量仍是 -φ / ω”。
- 复测未捕获 Applet 运行时 error；平台 iframe 仍有既有 sandbox warning，属于平台嵌入配置风险，不是本资源脚本错误。

数学口径保持不变：

- 振幅为 `|A|`。
- `A < 0` 表示关于 x 轴翻折。
- 一般周期为 `2π / |ω|`，本资源主线限制 `ω > 0`。
- 水平平移量为 `-φ / ω`。

本轮仍不建议进入 `classroom_trial`、`release_candidate` 或 `published`；建议状态为 `ready_for_teacher_trial_with_browser_risk`，即可以交真实教师试读 18 分钟主片段，但仍需总控确认 iframe 首屏风险是否已满足课堂投屏标准。

## 2026-05-09 数学与交互审校结论（历史记录）

2026-05-09 只审校已实现资源包 `content/applets/SH-HS-MATH-HJ-B2-C07-L05-A01/**`，未修改课程索引、生产 backlog 或其他资源包。数学表达、变换顺序、交互目标和平台 iframe 可见性已完成一轮复核与修订；当时建议下一状态进入 `math_review`，由数学审校者复核后再进入教学节奏试读。

## 2026-05-09 审校证据

- 函数对象限定为 `y = A sin(ωx + φ)`，默认不讨论竖直平移。
- 振幅写为 `|A|`；`A < 0` 明确为关于 x 轴翻折，并补充最大值点和最小值点会对调的审校口径。
- 周期使用一般式 `2π / |ω|`；本资源滑块限制 `0.5 <= ω <= 3`，因此课堂主线写清 `ω > 0`，界面显示 `2π / ω`。
- 相位/水平平移量写为 `-φ / ω`，并在界面、脚本、学生活动中强调不能把 `φ` 直接当作平移量。
- 关键点追踪口径为 `ωx + φ = kπ` 与 `ωx + φ = π/2 + kπ`，并说明 `A < 0` 时最值类别对调。
- 变换顺序呈现已区分中间曲线：先伸缩后平移显示 `y = sin(ωx)`，先平移后伸缩显示 `y = sin(x + φ)`；最终曲线相同，但最终平移量仍为 `-φ / ω`。
- 页面可见文本使用课堂符号 `π`、`ω`、`φ`、`2π / |ω|`、`sin x` 等数学语言。

## 2026-05-09 修改内容

- `src/index.html`
  - 将界面中的 `omega`、`phi` 改为 `ω`、`φ`，补充 `ω > 0` 边界说明。
  - 修正中间曲线函数，使两种变换顺序分别呈现 `sin(ωx)` 与 `sin(x + φ)`。
  - 周期计算改为 `2π / |ω|`，并保留本资源 `ω > 0` 的显示口径。
  - 对外部初始化传入的 `ω` 做正值范围归一，避免资源边界被绕过。
  - 调整 iframe 宽度断点，使平台预览首屏保持图像区与控制区并排。
- `metadata.yaml`
  - 补全 `2π / |ω|`、`-φ / ω`、`ω > 0`、`A < 0` 最值对调等数学边界。
  - 同步教学问题、学习目标、交互说明和反馈标签中的课堂符号。
- `README.md`
  - 同步资源边界、参数范围和交互能力描述。
- `teacher-script.md`
  - 增加 `ω > 0` 主线边界、一般周期公式和 `A < 0` 最值追问。
  - 强化 `φ` 不是最终平移量、必须除以 `ω` 的课堂追问。
- `student-task.md`
  - 同步 `ω`、`φ`、`π` 表达，加入 `2π / |ω|` 与 `ω > 0` 的活动提示。
- `review.md`
  - 更新本轮审校证据、修改内容、浏览器验证、剩余风险和下一状态建议。

## 2026-05-09 浏览器检查

检查地址：`http://127.0.0.1:5174/#lesson=SH-HS-MATH-HJ-B2-C07-L05`

- Browser 插件在本轮后段未暴露可调用接口，改用同一浏览器内核的 Playwright/Chrome headless 脚本补充验证。
- 平台页加载成功，课时为 `7.3 函数 y = A sin(ω x + φ) 的图像`，资源 iframe 数量为 1，iframe 内标题为“三角函数参数变化实验室”。
- 1280x900 视口中，滚动到资源预览后，图像区与控制区并排显示；图像区约 `466px` 宽，控制区约 `350px` 宽，未见遮挡。
- 交互读回：设 `ω = 2`、`φ = π/2` 后，周期显示为 `π`，平移显示为 `-π/4`；切换“平移后伸缩”后说明文本为“先得到 y = sin(x + φ)，再做横向伸缩；最终平移量仍是 -φ / ω。”
- 真实鼠标拖动观察点读回：`x` 从 `0π` 变为约 `1.16π`，当前函数值同步更新为约 `0.51`。
- 控制台无 app-origin error；仅有 Vite dev 连接日志和平台 iframe sandbox 警告。

## 2026-05-09 验证结果

- `npm run validate:content`：通过。
- `npm run verify`：通过；包含 `validate:content`、`generate:backlog`、Node test 28 项和 Vite build。构建仍有 Vite 单 chunk 超过 500 kB 的既有提示，不影响本资源审校结论。

## 剩余风险

- 18 分钟主片段尚未由真实教师限时试读，仍需确认学生记录量和暂停点。
- 第二片段不应塞回主片段；若课堂只给 12 到 18 分钟，应优先保留 A、ω、φ 单参数观察。
- `metadata.yaml` 中 `pedagogy.estimated_classroom_minutes` 仍为 25；本任务允许范围不含 metadata，建议总控后续决定是否单独同步。
- 尚未在真实触控屏或教室大屏上验证拖动手感。
- `ω < 0` 的等价改写仍建议保留为拓展，不进入本资源主线。

## 下一状态建议

建议保持数学审校通过方向，但课堂维度只建议进入真实教师试读准备，不进入 `classroom_trial`。进入更高状态前，还需完成真实教师 18 分钟试读、iframe 首屏复测和真实触控环境检查。
