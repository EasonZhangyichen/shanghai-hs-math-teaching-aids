# 学生活动：参数变化实验室

适用资源：`SH-HS-MATH-HJ-B2-C07-L05-A01`
完成方式：课堂观察记录、小组讨论、个人短答

## 活动目标

- 说明 `A`、`omega`、`phi` 分别影响振幅、周期和相位平移。
- 解释 `omega` 与周期的反向关系。
- 解释相位平移量为什么是 `-phi / omega`。
- 根据图像关键点反推参数。

## 任务一：单独改变 A

保持 `omega = 1`、`phi = 0`，调节 `A`。

| A | 最高点 | 最低点 | 是否翻折 | 振幅 |
| --- | --- | --- | --- | --- |
| 1 |  |  |  |  |
| 2 |  |  |  |  |
| -1 |  |  |  |  |

归纳：

```text
A 控制：______________________________
振幅应写作：__________________________
A < 0 时：____________________________
```

## 任务二：单独改变 omega

保持 `A = 1`、`phi = 0`，调节 `omega`。

| omega | 一个周期长度 | 周期公式验证 |
| --- | --- | --- |
| 0.5 |  |  |
| 1 |  |  |
| 2 |  |  |

回答：

1. `omega` 变大时，周期怎样变化？
2. 如果图像周期是 `pi`，`omega` 是多少？
3. 为什么不能直接说 `omega` 就是周期？

## 任务三：单独改变 phi

保持 `A = 1`，先令 `omega = 1`，再令 `omega = 2`，比较同一个 `phi`。

| omega | phi | 平移量 `-phi / omega` | 图像方向 |
| --- | --- | --- | --- |
| 1 | `pi/2` |  |  |
| 2 | `pi/2` |  |  |
| 1 | `-pi/2` |  |  |

回答：

1. 为什么括号里是 `+phi`，图像却可能向左移动？
2. 为什么 `omega` 改变后，同一个 `phi` 的平移量也会改变？

## 任务四：变换顺序比较

切换两种变换顺序，观察中间曲线。

| 顺序 | 第一步表达式 | 第二步表达式 | 最终表达式 |
| --- | --- | --- | --- |
| 先横向伸缩后平移 |  |  |  |
| 先平移后横向伸缩 |  |  |  |

思考：

- 两种顺序的中间曲线是否相同？
- 最终曲线是否相同？
- 哪一步最容易导致平移量判断错误？

## 任务五：从图像反推参数

观察当前图像，按顺序回答：

```text
1. 振幅 |A| = ________________________
2. 周期 T = __________________________
3. omega = ___________________________
4. 一个零点或最值点坐标：____________
5. 相位平移量 -phi / omega = __________
6. 可能的 A, omega, phi：_____________
```

## 快速诊断

判断下列说法是否正确，并说明理由。

1. `omega` 越大，函数周期越大。
2. `y = sin(2x + pi/2)` 的平移量是 `-pi/2`。
3. `A = -2` 时，振幅是 `2`，图像关于 x 轴翻折。
4. 从图像反推参数时，应先看相位，再看振幅和周期。

## 提交要求

每组提交一个“最容易错的参数”并给出图像证据：

```text
参数：______________________________
容易错在：__________________________
图像证据：__________________________
正确表达：__________________________
```
