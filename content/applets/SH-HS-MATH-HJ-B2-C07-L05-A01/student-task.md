# 学生活动：参数变化实验室

适用资源：`SH-HS-MATH-HJ-B2-C07-L05-A01`
完成方式：课堂观察记录、小组讨论、个人短答
建议使用：18 分钟主片段只完成“必做任务”；“可选延伸”用于第二个 12 到 15 分钟片段

## 活动目标

- 说明 `A`、`ω`、`φ` 分别影响振幅、周期和水平平移。
- 解释振幅为什么是 `|A|`，并说明 `A < 0` 时图像关于 x 轴翻折。
- 解释 `ω` 与周期的反向关系。本资源限制 `ω > 0`，因此周期分式的分母可写 `ω`；一般式的分母应写 `|ω|`。
- 解释水平平移量为什么是分子为 `φ`、分母为 `ω`，前面取负号。

## 必做任务一：A 控制振幅与翻折

保持 `ω = 1`、`φ = 0`，调节 `A`。

| A | 最高点变化 | 最低点变化 | 是否关于 x 轴翻折 | 振幅 |
| --- | --- | --- | --- | --- |
| 2 |  |  |  |  |
| -1 |  |  |  |  |

归纳：

```text
振幅应写作：__________________________
A < 0 时：____________________________
```

## 必做任务二：ω 控制周期

保持 `A = 1`、`φ = 0`，调节 `ω`。

| ω | 一个周期长度 | 周期公式验证 |
| --- | --- | --- |
| 2 |  |  |
| 0.5 |  |  |

回答：

```text
ω 变大时，周期怎样变化？__________________________
一般周期公式：____________________________________
本资源为什么可以把周期分式的分母写成 ω？_________
```

## 必做任务三：φ 的平移量要除以 ω

比较同一个 `φ = π/2` 在不同 `ω` 下的水平平移量。

| ω | φ | 平移量：分子 φ、分母 ω，前面取负号 | 图像方向 |
| --- | --- | --- | --- |
| 1 | `π/2` |  |  |
| 2 | `π/2` |  |  |

回答：

```text
为什么不能只看 φ 的符号判断平移？________________
为什么同一个 φ，在 ω 改变后平移量也改变？________
```

## 必做任务四：出口反推

观察教师给出的当前图像，只做一组口头或书面反推。

```text
1. 振幅 |A| = ________________________
2. 周期 T = __________________________
3. ω = ______________________________
4. 一个零点或最值点坐标：____________
5. 水平平移量 = ______________________
```

提交一句话：

```text
我反推参数时，先看 __________，再看 __________，最后看 __________。
```

## 可选延伸：变换顺序比较

仅在第二个课堂片段使用。切换两种变换顺序，观察中间曲线。

| 顺序 | 中间曲线 | 最终曲线是否相同 |
| --- | --- | --- |
| 先横向伸缩后平移 |  |  |
| 先平移后横向伸缩 |  |  |

思考：

- 两种顺序的中间曲线是否相同？
- 哪一步最容易导致平移量判断错误？
- 为什么最终仍要回到“分子 φ、分母 ω，前面取负号”的平移量？

## 快速诊断

判断下列说法是否正确，并说明理由。主片段只选 2 题，第二片段可补做其余题。

1. `ω` 越大，函数周期越大。
2. `y = sin(2x + π/2)` 的水平平移量是负的二分之 π。
3. `A = -2` 时，振幅是 `2`，图像关于 x 轴翻折。
4. 从图像反推参数时，应先看相位，再看振幅和周期。

## 提交要求

每组只提交一个“最容易错的参数”并给出图像证据：

```text
参数：______________________________
容易错在：__________________________
图像证据：__________________________
正确表达：__________________________
```
