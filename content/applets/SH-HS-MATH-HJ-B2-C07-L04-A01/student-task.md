# 学生活动：比较余弦函数性质

适用资源：`SH-HS-MATH-HJ-B2-C07-L04-A01`
完成方式：课堂观察记录、小组讨论、个人短答

## 活动目标

- 比较正弦函数和余弦函数的关键点顺序。
- 从余弦图像证据写出周期性、单调性和对称性。
- 用 `k in Z` 写出余弦函数零点、最值点和单调区间的一般形式。

## 任务一：先分清正弦参照和余弦主图像

移动同步游标，填写表格。

| `x` | `cos x` | `sin x` | 余弦图像证据 |
| --- | --- | --- | --- |
| `0` |  |  |  |
| `pi/2` |  |  |  |
| `pi` |  |  |  |
| `3pi/2` |  |  |  |
| `2pi` |  |  |  |

回答：

1. 余弦函数在 `x = 0` 处为什么不是 `0`？
2. 如果把正弦关键点顺序直接搬到余弦，会错在哪里？

## 任务二：周期比较

打开周期层，观察当前余弦点 `P = (x, cos x)` 和周期比较点。

| 当前横坐标 | 比较点横坐标 | 两点横坐标差 | 两点纵坐标关系 |
| --- | --- | --- | --- |
| `x` | `x + 2pi` 或 `x - 2pi` |  |  |

归纳：

```text
cos(x + ____________) = cos x
最小正周期：________
```

## 任务三：单调区间端点

打开单调层，分别观察 `k = -1, 0, 1`。

| `k` | 递增区间 | 递减区间 |
| --- | --- | --- |
| `-1` |  |  |
| `0` |  |  |
| `1` |  |  |

归纳：

```text
递增区间：______________________________
递减区间：______________________________
```

检查问题：

1. 为什么 `[0, pi]` 对余弦函数是递减区间？
2. 为什么一般式中要出现 `2kpi`？

## 任务四：对称元素分类

打开对称层，把下列对象放入正确类别。

| 对象 | 对称轴 | 对称中心 | 偶函数配对证据 |
| --- | --- | --- | --- |
| `x = 0` |  |  |  |
| `(pi/2, 0)` |  |  |  |
| `x = pi` |  |  |  |
| `(3pi/2, 0)` |  |  |  |
| `cos(-x) = cos x` |  |  |  |

归纳：

```text
偶函数关系：__________________________
对称轴：______________________________
对称中心：____________________________
```

## 任务五：关键点一般式

打开关键点层，先写特殊点，再推广为一般式。

| 类型 | 特殊点举例 | 一般式 |
| --- | --- | --- |
| 零点 | `(pi/2, 0)`, `(3pi/2, 0)` |  |
| 最大值点 | `(0, 1)`, `(2pi, 1)` |  |
| 最小值点 | `(pi, -1)` |  |

提示：一般式中必须说明 `k in Z`。

## 任务六：快速诊断

判断下列说法是否正确，并说明理由。

1. 余弦函数是偶函数。
2. 余弦函数的所有零点可以写成 `x = kpi`。
3. 余弦函数在 `[0, pi]` 上递减。
4. 余弦函数的最大值点可以写成 `(2kpi, 1)`。
5. 余弦函数的对称中心可以写成 `(pi/2 + kpi, 0)`。

## 提交要求

每组提交两条最容易套错正弦性质的余弦结论，并写出对应图像证据：

```text
余弦性质：____________________________
图像证据：____________________________
一般式：______________________________
```
