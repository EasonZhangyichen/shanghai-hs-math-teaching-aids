# 审核记录：正弦余弦性质对照板

资源 ID：`SH-HS-MATH-HJ-B2-C07-L04-A01`
版本：`0.1.0`
当前状态：`draft`
最后更新：2026-05-08

## 本轮审校结论

本轮只审校 `SH-HS-MATH-HJ-B2-C07-L04-A01` 资源包。已修正交互原型、元数据、教师脚本和学生活动中的正弦/余弦性质对照表达，并完成浏览器交互检查、内容校验和仓库级验证。

数学结论：正弦、余弦两列均已完整呈现定义域 `R`、值域 `[-1, 1]`、周期 `2π`、奇偶性、零点、最值点、单调区间、对称轴和对称中心，且一般式均注明 `k ∈ Z`。余弦性质已按本课口径区分为零点 `x = π/2 + kπ`、最大值点 `(2kπ, 1)`、最小值点 `(π + 2kπ, -1)`、对称轴 `x = kπ`、对称中心 `(π/2 + kπ, 0)`，未与正弦性质混用。

推荐状态：建议进入 `math_review`。进入 `classroom_trial` 前仍建议由一线教师试读 20 分钟课堂节奏。

## 数学审校清单

- [x] 明确正弦函数 `y = sin x` 和余弦函数 `y = cos x` 的定义域为 `R`，值域为 `[-1, 1]`，最小正周期为 `2π`。
- [x] 明确正弦函数为奇函数：`sin(-x) = -sin x`；余弦函数为偶函数：`cos(-x) = cos x`。
- [x] 明确正弦零点 `x = kπ`、最大值点 `(π/2 + 2kπ, 1)`、最小值点 `(-π/2 + 2kπ, -1)`，`k ∈ Z`。
- [x] 明确正弦单调递增区间为 `[-π/2 + 2kπ, π/2 + 2kπ]`，递减区间为 `[π/2 + 2kπ, 3π/2 + 2kπ]`，`k ∈ Z`。
- [x] 明确正弦对称轴为 `x = π/2 + kπ`，对称中心为 `(kπ, 0)`，`k ∈ Z`。
- [x] 明确余弦函数为偶函数：`cos(-x) = cos x`。
- [x] 明确余弦单调递增区间为 `[-π + 2kπ, 2kπ]`，递减区间为 `[2kπ, π + 2kπ]`，`k ∈ Z`。
- [x] 明确余弦对称轴为 `x = kπ`，对称中心为 `(π/2 + kπ, 0)`，`k ∈ Z`。
- [x] 明确余弦零点为 `x = π/2 + kπ`，最大值点为 `(2kπ, 1)`，最小值点为 `(π + 2kπ, -1)`，`k ∈ Z`。
- [x] 教师脚本已修正“把 `(kπ, 0)` 写成余弦对称中心”的纠错话术：`x = kπ` 是余弦对称轴，余弦对称中心在零点处。
- [x] 数学审校者仍可复核单调区间端点闭区间写法是否与本校教材板书口径完全一致。
- [x] 对称中心一般式已作为正弦/余弦对照项显性呈现，适合作为图像观察补充。

## 教学审校清单

- [x] 对应沪教版必修第二册第 7 章 7.2.2“余弦函数的性质”。
- [x] 聚焦课程图谱中记录的三个痛点：正弦性质套用、关键点一般式漏周期、单位圆和图像解释断裂。
- [x] 一个资源只聚焦“比较”这一主要认知动作。
- [x] 教师脚本包含正弦参照、周期、单调、对称、关键点和收束检查。
- [x] 学生活动包含观察、记录、归纳、判断和表达。
- [ ] 需由一线教师确认 20 分钟课堂节奏是否适合放在余弦图像课之后。

## 交互审校清单

- [x] 初始状态能显示余弦曲线、正弦参照曲线和同步游标。
- [x] 滑块、拖动和播放共用同一状态。
- [x] 周期段 `k` 切换能联动单调区间和性质表。
- [x] 分层开关支持周期、单调、对称、关键点和归纳逐步显示；归纳面板已改为正弦/余弦同表比较，而不是只堆叠余弦信息。
- [x] 支持关键角跳转、重置和 Applet SDK 基础消息。
- [x] 1280×720 浏览器视口下首屏核心曲线、同步游标、关键角跳转、周期段 `k`、图层开关和反馈卡片可见，无截断或遮挡。
- [x] 关键角 `π/2` 跳转验证为 `cos x = 0.000`、`sin x = 1.000`；播放状态可推进同步游标。

## 浏览器与验证记录

- [x] `npm run validate:content` 通过：70 lessons、14 applets、4 Manim clips、7 diagnosis packages。
- [x] `npm run verify` 通过：内容校验、资源 backlog 生成、28 项 Node 测试、Web 构建均完成；构建仍有既有的 chunk size warning。
- [x] Browser 插件可打开页面并完成按钮/表格 DOM 交互检查；截图接口本轮出现 `Page.captureScreenshot` 超时。
- [x] 使用终端 Playwright 作为截图回退，完成 1280×720 截图、归纳面板快照、关键角跳转、播放/暂停和 console 检查；未发现页面脚本错误或警告。

## 版权与合规清单

- [x] 本草稿为原创脚本、任务与交互实现。
- [x] 未复制教材正文、官方 PPT、教案或商业平台资源。
- [x] 未引入外部代码、图片或第三方素材。
- [x] 当前实现使用原生 HTML/CSS/SVG/JavaScript，无第三方库 license 记录需求。

## 进入下一状态的门槛

进入 `math_review` 前至少需要：

1. 本轮已完成人工复核余弦单调区间、对称中心和关键点一般式。
2. 本轮已确认正弦参照曲线通过同表对照服务于余弦性质比较。
3. 本轮已完成浏览器交互一致性检查，覆盖播放、关键角跳转和图层开关。
4. 建议进入 `math_review`，由数学审校者做最终签核。

进入 `pedagogy_review` 前至少需要：

1. 数学审校无阻塞问题。
2. 教师脚本和学生活动根据试读反馈完成同步调整。
3. 大屏演示和触控操作无明显可用性问题。

## 待确认问题

- 进入 `classroom_trial` 前，一线教师需确认 20 分钟课堂节奏是否适合放在余弦图像课之后。
- 后续 `SH-HS-MATH-HJ-B2-C07-L04-D01` 可复用“正弦性质套用到余弦”的错因标签，但本轮未修改诊断资源。
