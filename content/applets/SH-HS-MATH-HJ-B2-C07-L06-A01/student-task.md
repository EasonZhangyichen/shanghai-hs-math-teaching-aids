# 学生活动：正切函数图像生成器

适用资源：`SH-HS-MATH-HJ-B2-C07-L06-A01`
完成方式：课堂观察记录、小组讨论、个人短答

## 活动目标

- 用单位圆终边斜率解释 `tan x`。
- 写出正切函数的定义域和渐近线。
- 描述靠近渐近线左右两侧时的变化趋势。
- 解释正切函数周期为 `pi`。

## 任务一：从单位圆读出正切值

拖动角度，观察单位圆终边和图像点。

| x | sin x | cos x | tan x = sin x / cos x | 图像点 |
| --- | --- | --- | --- | --- |
| `-pi/4` |  |  |  |  |
| `0` |  |  |  |  |
| `pi/4` |  |  |  |  |

归纳：

```text
tan x 表示：__________________________
当 cos x = 0 时：_____________________
```

## 任务二：找出不能取的 x

观察渐近线位置，补全表格。

| 渐近线 | 对应 cos x | tan x 是否有定义 | 原因 |
| --- | --- | --- | --- |
| `-pi/2` |  |  |  |
| `pi/2` |  |  |  |
| `3pi/2` |  |  |  |

写出一般式：

```text
正切函数定义域：x ____________________
渐近线：x = _________________________
```

## 任务三：逼近渐近线

切换到“逼近渐近线”，分别从 `pi/2` 左右两侧靠近。

| 位置 | cos x 的符号 | tan x 的变化 | 趋向 |
| --- | --- | --- | --- |
| `pi/2` 左侧 |  |  |  |
| `pi/2` 右侧 |  |  |  |

回答：

1. 为什么越靠近渐近线，图像越陡？
2. 渐近线是不是图像的一部分？为什么？
3. 左右两侧趋势为什么不同？

## 任务四：周期对照

打开 `x + pi` 对照点，拖动不同的 `x`。

| x | tan x | x + pi | tan(x + pi) | 是否相等 |
| --- | --- | --- | --- | --- |
| `-pi/4` |  |  |  |  |
| `0` |  |  |  |  |
| `pi/4` |  |  |  |  |

归纳：

```text
tan(x + pi) 与 tan x 的关系：____________
正切函数的最小正周期：__________________
```

## 快速诊断

判断下列说法是否正确，并说明理由。

1. 正切函数图像可以在每条渐近线处连起来。
2. `x = pi/2` 处没有函数值，因为 `cos x = 0`。
3. 正切函数的周期是 `2pi`。
4. 靠近 `pi/2` 左侧时，`tan x` 趋向正无穷。

## 提交要求

每组提交一个“断开图像”的解释：

```text
不能取的 x：__________________________
原因：_______________________________
图像表现：___________________________
正确画法：___________________________
```
