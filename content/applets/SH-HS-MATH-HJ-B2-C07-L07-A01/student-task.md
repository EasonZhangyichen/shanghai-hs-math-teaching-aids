# 学生活动：正切函数性质比较

## 活动 1：单调性必须分段说

拖动 `x`，只观察高亮区间 `(-π/2, π/2)`。

| x 从左到右移动 | tan x 的变化 | 能否跨过渐近线继续比较 |
| --- | --- | --- |
|  |  |  |

把结论补全：

```text
y = tan x 在每个区间 ______________________________ 上单调递增。
不能说它在整个定义域上单调递增，因为 ______________________________。
```

## 活动 2：周期对照

打开“周期对照”，选择三个不同的 `x` 观察。

| x | tan x | x + π | tan(x + π) | 关系 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |

写出结论：

```text
tan(x + π) = __________
y = tan x 的周期是 __________
```

## 活动 3：奇函数对照

打开“奇对称”，观察 `x` 与 `-x`。

| x | tan x | -x | tan(-x) | 两点关系 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |
|  |  |  |  |  |

写出结论：

```text
tan(-x) = __________
y = tan x 是 __________ 函数，图像关于 __________ 对称。
```

## 活动 4：值域和渐近线

只看一个连续分支，拖动点靠近左右两端。

| 位置 | 函数值趋势 | 是否能跨过渐近线 |
| --- | --- | --- |
| 接近左端 |  |  |
| 接近右端 |  |  |

解释：

```text
虽然 x = π/2 + kπ, k ∈ Z 处有渐近线，但每个分支都从 __________ 延伸到 __________，
所以 y = tan x 的值域是 __________。
```

## 自查

- 我能写出正切函数定义域。
- 我能把单调区间写成一般式。
- 我能解释周期为什么是 `π`。
- 我能用点对说明正切函数是奇函数。
