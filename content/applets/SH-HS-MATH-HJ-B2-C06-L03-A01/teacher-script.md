# 教师脚本：单位圆三角比与终边联动板

1. 拖动 `P`，确认蓝色射线是角的终边，`P` 是终边与单位圆的交点。
2. 观察 `P = (cos θ, sin θ)`，说明横坐标给出 `cos θ`，纵坐标给出 `sin θ`。
3. 点击四象限代表角，对照投影段颜色和象限符号卡。
4. 停在 `π/2` 与 `3π/2`，追问为什么 `cos θ = 0` 时 `tan θ` 无定义。

板书：

```text
P = (cos θ, sin θ)
tan θ = sin θ / cos θ
cos θ = 0 时 tan θ 无定义
```
