# 单位圆三角比与终边联动板

资源 ID：`SH-HS-MATH-HJ-B2-C06-L03-A01`

当前状态：可运行 HTML Applet 草稿，建议保持 `self_checked_draft`。

拖动单位圆上的点 `P` 改变角的终边。界面同步展示单位圆上的点 `P = (cos θ, sin θ)`、横向有向段 `cos θ = x`、纵向有向段 `sin θ = y`、切线 `x = 1` 上的 `tan θ` 有向高度，以及四象限符号卡的同步高亮。

关键角使用 `π` 的上下分式排版，不使用 ASCII `pi/2`。
