# 学生活动：数乘伸缩实验室

## 活动目标

通过拖动原向量 `a` 和调节实数 `λ`，说明 `λa` 的方向与长度怎样由 `λ` 决定，并能单独处理 `λ = 0` 的零向量边界。

## 任务 1：正数倍

把 `λ` 调到 `0.5`、`1`、`2`，观察橙色 `λa`。

记录：

| λ | λa 与 a 的方向关系 | λa 的长度是 a 的几倍 |
| --- | --- | --- |
| 0.5 |  |  |
| 1 |  |  |
| 2 |  |  |

用一句话概括 `λ > 0` 时的规律：

```text

```

## 任务 2：零向量边界

把 `λ` 调到 `0`。

回答：

1. `λa` 的长度是多少？
2. 此时能不能说 `λa` 与 `a` 同向或反向？为什么？

结论：

```text

```

## 任务 3：负数倍

把 `λ` 调到 `-1`、`-0.5`。再拖动蓝色原向量 `a` 的端点，换一个方向后重复观察。

记录：

| λ | λa 与 a 的方向关系 | λa 的长度是 a 的几倍 |
| --- | --- | --- |
| -1 |  |  |
| -0.5 |  |  |

用一句话概括 `λ < 0` 时的规律：

```text

```

## 任务 4：完整表述

补全下面的课堂结论。

```text
当 a 不是零向量时：
λ > 0，λa 与 a __________，长度为 __________；
λ = 0，λa 是 __________，方向 __________；
λ < 0，λa 与 a __________，长度为 __________。
```

附加追问：

如果 `a` 本身是零向量，那么无论 `λ` 取什么值，`λa` 是什么向量？方向是否规定？
