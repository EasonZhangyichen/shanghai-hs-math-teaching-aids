# 审核记录：正弦与余弦相位对照

资源 ID：`SH-HS-MATH-HJ-B2-C07-L03-A02`
版本：`0.1.0`
当前状态：`draft`
最后更新：2026-05-07

## 草稿自检结论

该资源包已创建 metadata、README、教师脚本、学生活动、审核记录和 `src/index.html` 可运行原型。当前保持 `draft`，用于数学边界复核、课堂节奏试读和浏览器交互验证；尚未进入数学审校。

## 数学审校清单

- [x] 明确同一角 `x` 下 `sin x` 来自单位圆动点纵坐标，`cos x` 来自横坐标。
- [x] 明确关键角处正弦值序列为 `0, 1, 0, -1, 0`，余弦值序列为 `1, 0, -1, 0, 1`。
- [x] 明确相位关系为 `cos x = sin(x + pi/2)`。
- [x] 使用 `x = 0` 检查相位方向，避免误写成 `sin(x - pi/2)`。
- [ ] 数学审校者需复核相位参照点在延拓区间中的显示口径。
- [ ] 数学审校者需确认是否在本课时使用“左移 pi/2”语言，或仅保留 `x + pi/2` 的函数值对应。

## 教学审校清单

- [x] 对应沪教版必修第二册第 7 章 7.2.1“余弦函数的图像”。
- [x] 聚焦课程图谱中记录的三个痛点：机械平移记忆、关键点顺序混用、相位方向混淆。
- [x] 一个资源只聚焦“比较”这一主要认知动作。
- [x] 教师脚本包含同角坐标来源、关键角对照、相位参照和收束检查。
- [x] 学生活动包含观察、记录、验证、判断和表达。
- [ ] 需由一线教师确认 16 分钟课堂节奏是否适合放在余弦图像生成之后。

## 交互审校清单

- [x] 初始状态能同时显示单位圆和两条函数曲线。
- [x] 滑块、拖动和播放共用同一状态。
- [x] 同步游标能在同一 `x` 下读出 `sin x` 与 `cos x`。
- [x] 相位参照点能显示正弦曲线上 `x + pi/2` 处的值。
- [x] 支持关键角跳转、重置和图层切换。
- [ ] 需完成大屏可读性、触控拖动和普通浏览器兼容性验证。

## 版权与合规清单

- [x] 本草稿为原创脚本、任务与交互实现。
- [x] 未复制教材正文、官方 PPT、教案或商业平台资源。
- [x] 未引入外部代码、图片或第三方素材。
- [x] 当前实现使用原生 HTML/CSS/SVG/JavaScript，无第三方库 license 记录需求。

## 进入下一状态的门槛

进入 `math_review` 前至少需要：

1. 人工复核关键角、相位参照和结论表达。
2. 确认“左移 pi/2”与 `sin(x + pi/2)` 的课堂语言不会造成方向误解。
3. 完成浏览器交互一致性检查，特别是拖动、播放、关键角跳转和相位参照。
4. 由一线教师试读 16 分钟课堂节奏。

进入 `pedagogy_review` 前至少需要：

1. 数学审校无阻塞问题。
2. 教师脚本和学生活动根据试读反馈完成同步调整。
3. 大屏演示和触控操作无明显可用性问题。

## 待确认问题

- 是否把 `cos x = sin(x + pi/2)` 的图像语言表述为“余弦图像对应正弦图像向左平移 pi/2”，还是先保留函数值对应。
- 是否需要在后续 `SH-HS-MATH-HJ-B2-C07-L04-A01` 复用同一套正弦/余弦颜色语义。
