# 学生活动：向量加减法构造器

## 任务一：拖动并记录

1. 拖动点 `A`，记录向量 `a` 的变化。
2. 拖动点 `B`，记录向量 `b` 的变化。
3. 拖动点 `O`，观察 `a`、`b`、`a + b`、`a - b` 是否改变。

记录：

- 我改变的是：`O` / `A` / `B`
- 向量 `a` 的方向和大致大小：
- 向量 `b` 的方向和大致大小：
- `a + b` 的端点路径：
- `a - b` 的端点路径：

## 任务二：比较两种加法构造

在同一组 `a`、`b` 下，分别切换「首尾相接」和「平行四边形」。

回答：

- 首尾相接时，结果向量从哪里出发，到哪里结束？
- 平行四边形时，对角线从哪里出发，到哪里结束？
- 两种构造得到的 `a + b` 是否相同？用画面证据说明。

## 任务三：解释减法方向

切换到「减法与相反向量」。

回答：

- `-b` 和 `b` 的方向有什么关系？
- 为什么 `a - b` 可以看成 `a + (-b)`？
- 共起点时，从 `B` 指向 `A` 的向量表示什么？
- 从 `A` 指向 `B` 的向量又表示什么？

## 归纳句

请补全：

- 向量加法的首尾相接法则强调：先走 `____`，再走 `____`，结果是从 `____` 指向 `____`。
- 平行四边形法则中的对角线表示：`____`。
- 向量减法的关键是：把减法转化为加上 `____`。
