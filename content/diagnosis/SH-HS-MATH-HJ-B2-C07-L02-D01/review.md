# 审核记录

资源 ID：`SH-HS-MATH-HJ-B2-C07-L02-D01`

当前状态：`draft`

审核状态：`self_checked_draft`

## 自检结论

- 已对齐课时 `SH-HS-MATH-HJ-B2-C07-L02`「正弦函数的性质」。
- 已对齐认知动作「比较」。
- 已覆盖三个教学痛点：单调区间端点、对称轴/中心混淆、零点和最值点周期遗漏。
- 已创建 `metadata.yaml`、`README.md`、`item-bank.yaml`、`scoring-rubric.md`、`teacher-notes.md`。
- 题组为原创设计，不复制教材正文、官方课件、教案或商业题库。

## 数学自检

- 定义域：`R`。
- 值域：`[-1, 1]`。
- 最小正周期：`2π`。
- 奇偶性：`sin(-x) = -sin x`，所以 `y = sin x` 是奇函数。
- 零点一般式：`x = kπ`，`k ∈ Z`。
- 最大值点横坐标：`x = π/2 + 2kπ`，`k ∈ Z`；最小值点横坐标：`x = -π/2 + 2kπ`，`k ∈ Z`。
- 递增区间：`[-π/2 + 2kπ, π/2 + 2kπ]`，`k ∈ Z`。
- 递减区间：`[π/2 + 2kπ, 3π/2 + 2kπ]`，`k ∈ Z`。
- 对称轴：`x = π/2 + kπ`，`k ∈ Z`。
- 对称中心：`(kπ, 0)`，`k ∈ Z`。

## 2026-05-08 数学审校与试做

试做结论：

- 6 题仍保持 10 分、约 8 分钟完成；Q01 和 Q04 为多选，Q05 和 Q06 接受短句或条目式说明。题量可用于新授课末尾即时诊断，但学生书写较慢时 Q06 适合只要求订正两类错误。
- 数学结论复核通过：定义域 `R`、值域 `[-1, 1]`、最小正周期 `2π`、奇函数、零点、最值点、单调区间、对称轴和对称中心均已按完整一般式表达，并注明 `k ∈ Z`。
- 递增区间使用闭区间 `[-π/2 + 2kπ, π/2 + 2kπ]`，递减区间使用闭区间 `[π/2 + 2kπ, 3π/2 + 2kπ]`，符合正弦函数在连续闭区间上的单调表述；区间端点和周期平移未遗漏。

修改内容：

- 将题组可见数学文本统一为 `π`、`2π` 和 `k ∈ Z`，避免“k 属于整数”与 ASCII `pi` 在课堂端显示不统一。
- 将 Q01 改为基础性质与零点多选题，显式检查定义域、值域、最小正周期 `2π`、奇函数和零点一般式，并新增 `period_length_confusion` 错因标签，用于区分“相邻零点间隔 π”与“最小正周期 2π”混淆。
- 将 Q02 改为同时检查最大值点和最小值点横坐标，补齐最小值点 `x = -π/2 + 2kπ，k ∈ Z`。
- 修正 Q04 的错误选项：原 `(π/2 + kπ, 1)` 在奇数 `k` 时不在图像上，现改为最大值点 `(π/2 + 2kπ, 1)`，使错因只指向“最值点不是对称中心”。
- 在 Q06 中补入“最小正周期误写为 π”的订正点，并同步更新 `metadata.yaml`、`scoring-rubric.md`、`teacher-notes.md` 和 `README.md` 的题量、错因标签、评分说明和讲评路径。

文件一致性检查：

- `metadata.yaml` 与 `item-bank.yaml` 均为 6 题、预计 8 分钟，错因标签一致包含 `period_omission`、`period_length_confusion`、`monotonic_endpoint_confusion`、`axis_center_mixup`、`extrema_zero_formula_omission` 和 `local_interval_overgeneralization`。
- `item-bank.yaml` 总分为 10 分；`scoring-rubric.md` 的题号、分值、掌握阈值 7.5 分、部分得分规则和错因优先级与题组一致。
- `teacher-notes.md` 的讲评路径覆盖周期遗漏、周期长度混淆、单调端点、对称轴/中心、最值零点一般式和局部区间过度概括，能对应题组标签。

剩余风险：

- 尚未进行真实班级限时试做；Q01 三个正确项对基础薄弱学生可能略耗时，教师可在课堂中要求先判断 A/B/C，再追问 D 为什么错。
- 简答题和错误分析题后续接入诊断播放器时仍需教师端人工勾选错因标签，不能完全依赖自动判分。
- 本轮只做数学与文件一致性审校，尚未进行一线教师对 8 分钟讲评节奏的实测。

下一状态建议：

- 建议进入 `math_review`：本轮已完成数学表达、题组结构、错因标签和文件一致性审校。
- 暂不建议进入 `classroom_trial`：需要至少一次教师试读或小组限时试做，并确认简答/错误分析题的教师端标签记录方式。

## 待人工复核

- 纸质教材课时表述与本资源标题是否完全一致。
- 简答题参考答案是否适合本校课堂表达习惯。
- 错因标签统计阈值是否符合课堂时间。
- 多选题部分得分规则是否需要更细粒度记录。

## 后续推进建议

1. 总控或数学教师确认本轮修改后，可将资源推进到 `math_review`。
2. 教研组试读 8 分钟课堂流程，尤其观察 Q01 多选和 Q06 订正是否压缩得住。
3. 进入 `classroom_trial` 前补充真实学生样例或小组限时试做记录。
4. 诊断播放器落地后，将 `platform_card.availability` 从 `item_bank_ready` 升级到 `interactive_ready`。
