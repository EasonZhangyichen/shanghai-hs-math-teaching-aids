# 审核记录：正切函数性质诊断

资源 ID：`SH-HS-MATH-HJ-B2-C07-L07-D01`
当前状态：`draft`
当前阶段：`item_bank_ready`

## 自检结论

- 已对齐课时 `SH-HS-MATH-HJ-B2-C07-L07`「正切函数的性质」。
- 已对齐认知动作「比较」。
- 已覆盖三个教学痛点：整体单调误说、值域与渐近线矛盾感、周期/奇函数/单调区间一般式混淆。
- 已创建 `metadata.yaml`、`README.md`、`item-bank.yaml`、`scoring-rubric.md`、`teacher-notes.md`。
- 题组为原创设计，不复制教材正文、官方课件、教案或商业题库。

## 2026-05-08 数学审校与试做

试做结论：

- 6 题题量适合放在 Applet 分段观察之后完成；Q01 到 Q03、Q05 以选择题为主，Q04 和 Q06 接受短句或条目式订正，预计 7 到 8 分钟可完成，基础较好的班级可压缩到约 6 分钟。
- 数学结论复核通过：定义域排除 `x = π/2 + kπ，k ∈ Z`；竖直渐近线为 `x = π/2 + kπ，k ∈ Z`；最小正周期为 `π`；`tan(-x) = -tan x`，所以是奇函数；值域为 `R`；单调性只能写为在每个区间 `(-π/2 + kπ, π/2 + kπ)，k ∈ Z` 上递增，端点必须是开区间。
- Q02 与 Q04 已用跨分支反例强化“不能说整个定义域上递增”：例如 `π/4 < 3π/4`，但 `tan(π/4) = 1 > tan(3π/4) = -1`。

修改内容：

- 统一题干、选项、反馈、评分规则、教师说明和 README 中的正切性质表达，所有一般式均注明 `k ∈ Z`，并将 `pi`、`2pi` 等课堂可见文本改为 `π`、`2π`。
- 删除“趋向正无穷/负无穷”等偏重术语，改为“y 值可以任意小、任意大，并覆盖任意给定实数”的高中课堂表述。
- 补充 Q03 多选题漏选映射：漏选周期、奇函数或值域结论时分别记录 `period_pi_confusion`、`parity_symmetry_mixup` 或 `range_asymptote_conflict`。
- 将教师说明中的流程改为“6 到 8 分钟”，明确 Q04 和 Q06 可用短句或条目作答，避免书写负担过重。

文件一致性检查：

- `metadata.yaml` 与 `item-bank.yaml` 均为 6 题、预计 8 分钟，错因标签保持 6 类一致。
- `item-bank.yaml` 总分为 10 分；`scoring-rubric.md` 的题号、分值、掌握阈值 7.5 分和错因优先级与题组一致。
- `teacher-notes.md` 的讲评路径覆盖定义域遗漏、整体单调误说、区间端点错误、值域误解、周期混淆和奇偶性混淆，能对应题组标签。

剩余风险：

- 尚未进行真实班级限时试做；若学生书写慢，Q06 可在课堂中改为口头讲评或只要求订正两处错误。
- 简答题和错误分析题后续接入播放器时仍需教师端人工勾选错因标签，不能完全依赖自动判分。
- 资源仍需教研人员确认本校课堂是否接受“y 值可以任意小、任意大”的值域解释口径。

下一状态建议：

- 建议进入 `math_review`：本轮数学表达、题组结构和文件一致性已达到数学审校入口要求。
- 暂不建议进入 `classroom_trial`：需至少一次教师试读或小组限时试做，并确认 Diagnosis 播放器/教师看板对简答标签的处理方式。
