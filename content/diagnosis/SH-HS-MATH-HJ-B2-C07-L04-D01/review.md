# 审核记录

资源 ID：`SH-HS-MATH-HJ-B2-C07-L04-D01`

当前状态：`draft`

审核状态：`self_checked_draft`

## 自检结论

- 已对齐课时 `SH-HS-MATH-HJ-B2-C07-L04`「余弦函数的性质」。
- 已对齐认知动作「比较」。
- 已覆盖三个教学痛点：正弦性质误迁移、关键点一般式周期遗漏、单位圆和图像解释断裂。
- 已创建 `metadata.yaml`、`README.md`、`item-bank.yaml`、`scoring-rubric.md`、`teacher-notes.md`。
- 题组为原创设计，不复制教材正文、官方课件、教案或商业题库。

## 数学自检

- 定义域：`R`。
- 值域：`[-1, 1]`。
- 最小正周期：`2π`。
- 奇偶性：`cos(-x) = cos x`，`y = cos x` 是偶函数。
- 最大值点横坐标：`x = 2kπ`，`k ∈ Z`。
- 最小值点横坐标：`x = (2k + 1)π`，`k ∈ Z`。
- 零点横坐标：`x = π/2 + kπ`，`k ∈ Z`。
- 递减区间：`[2kπ, (2k + 1)π]`，`k ∈ Z`。
- 递增区间：`[(2k - 1)π, 2kπ]`，`k ∈ Z`。
- 对称轴：`x = kπ`，`k ∈ Z`。
- 对称中心：`(π/2 + kπ, 0)`，`k ∈ Z`。

## 2026-05-08 数学审校与试做

试做结论：

- 6 题总分仍为 10 分，课堂独立完成预计 7 到 8 分钟；Q01 和 Q04 为多选，Q05 和 Q06 接受短句或条目式说明。若学生书写速度偏慢，Q06 可要求“指出并改正至少两处错误”，不要求全量订正。
- 数学结论复核通过：定义域 `R`、值域 `[-1, 1]`、最小正周期 `2π`、偶函数、零点、最大值点、最小值点、单调区间、对称轴和对称中心均已按余弦函数性质表达，并注明 `k ∈ Z`。
- 递减区间使用闭区间 `[2kπ, (2k + 1)π]`，递增区间使用闭区间 `[(2k - 1)π, 2kπ]`，符合余弦函数在连续闭区间上的单调表述；端点、周期平移和方向未遗漏。

修改内容：

- 将 Q01 改为基础性质与零点多选题，显式检查定义域 `R`、值域 `[-1, 1]`、最小正周期 `2π`、偶函数和零点 `x = π/2 + kπ，k ∈ Z`。
- 将 Q02 改为同时检查最大值点 `x = 2kπ` 与最小值点 `x = (2k + 1)π`，并保留“把正弦最值点套到余弦”的错误选项。
- 在 Q06 中加入“最小正周期误写为 `π`”的订正点，区分相邻零点间隔和函数周期。
- 新增 `period_length_confusion` 错因标签；将原宽泛的对称混淆定位为 `axis_center_mixup`，使轴心混淆能被单独讲评。
- 将题组、评分规则、教师说明和 README 中的课堂可见数学表达统一为 `π`、`2π` 和 `k ∈ Z`，并同步更新题号分值、标签优先级和讲评路径。

文件一致性检查：

- `metadata.yaml` 与 `item-bank.yaml` 均为 6 题、预计 8 分钟，错因标签一致包含 `sine_transfer_error`、`period_omission`、`period_length_confusion`、`monotonic_interval_reversal`、`axis_center_mixup`、`zero_extrema_formula_mixup` 和 `unit_circle_graph_gap`。
- `item-bank.yaml` 总分为 10 分；`scoring-rubric.md` 的题号、分值、掌握阈值 7.5 分、部分得分规则和错因优先级与题组一致。
- `teacher-notes.md` 的讲评路径覆盖正弦迁移、周期遗漏、周期长度混淆、关键点一般式、单调区间方向、轴心混淆和单位圆图像转换断裂。

剩余风险：

- 尚未进行真实班级限时试做；Q01 三个正确项加一个干扰项，对基础薄弱学生可能略耗时。
- 简答题和错误分析题后续接入诊断播放器时仍需教师端人工勾选错因标签，不能完全依赖自动判分。
- 本轮只做数学与文件一致性审校，尚未进行一线教师对 6 到 8 分钟讲评节奏的实测。

下一状态建议：

- 建议进入 `math_review`：本轮已完成数学表达、题组结构、错因标签和文件一致性审校。
- 暂不建议进入 `classroom_trial`：需要至少一次教师试读或小组限时试做，并确认简答/错误分析题的教师端标签记录方式。

## 待人工复核

- 纸质教材课时表述与本资源标题是否完全一致。
- 简答题参考答案是否适合本校课堂表达习惯。
- 错因标签统计阈值是否符合课堂时间。
- 多选题部分得分规则是否需要更细粒度记录。

## 后续推进建议

1. 总控或数学教师确认本轮修改后，可将资源推进到 `math_review`。
2. 教研组试读 6 到 8 分钟课堂流程，尤其观察 Q01 多选和 Q06 订正是否压缩得住。
3. 进入 `classroom_trial` 前补充真实学生样例或小组限时试做记录。
4. 诊断播放器落地后，将 `platform_card.availability` 从 `item_bank_ready` 升级到 `interactive_ready`。
