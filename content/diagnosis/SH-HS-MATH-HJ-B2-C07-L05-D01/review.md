# 审核记录：参数识别诊断任务

资源 ID：`SH-HS-MATH-HJ-B2-C07-L05-D01`
当前状态：`draft`
当前阶段：`item_bank_ready`

## 自检结论

- 内容为原创题组、错因标签和反馈设计。
- 题组聚焦 `A`、`ω`、`φ` 的图像读数，不扩展竖直平移 `k`。
- 当前题组覆盖周期换算、相位平移、振幅翻折和综合候选式验证。
- 当前没有使用外部图片、教材正文、官方课件、教案或商业题库。
- 当前可用于平台题组摘要和教师错因预览；尚未接入 Diagnosis 播放器。

## 数学审校关注点

- 默认约定 `ω > 0` 是否在题干中说清楚，避免出现等价参数表达争议。
- Q03 中 `y = A sin(2x + π/2)` 的关键点判断是否足够支持 `A < 0`。
- Q05 用“过原点后先下降”判断 `A` 的符号是否适合当前学生水平。
- Q06 的干扰项是否能稳定区分周期、振幅和相位三个参数。

## 教学审校关注点

- 8 分钟内完成 6 题是否紧凑但可行。
- 简答题和错误分析题是否需要拆成课堂口答与书面订正两个版本。
- 错因标签是否能直接指向 Applet 或 Manim 的补救入口。
- 是否需要在教师说明中补充“先读振幅、再读周期、最后定相位”的板书模板。

## 平台接入关注点

- 当前 `platform_card.availability` 为 `item_bank_ready`，平台可先展示题组与错因摘要。
- 后续 Diagnosis 播放器需要支持单选、多选、简答和错误分析的混合评分。
- 简答题自动判分前，教师端应允许人工勾选命中的错因标签。

## 推进门槛

进入 `math_review` 前必须完成：

1. 教师或审校者完整试做 6 题。
2. 确认每个正确答案的参数读数理由无歧义。
3. 确认错因标签与 `teacher-notes.md` 中的讲评路径一致。
4. 确认 `metadata.yaml`、`item-bank.yaml` 和 `scoring-rubric.md` 的题量、分值、阈值一致。

进入 `classroom_trial` 前必须完成：

1. 用一个班级或小组试做，记录各错因标签命中率。
2. 根据实际用时调整题量或简答题要求。
3. 与 `SH-HS-MATH-HJ-B2-C07-L05-A01` 的 Applet 操作顺序对齐。
4. 若接入播放器，确认订正后不覆盖首次错因标签。

## 2026-05-09 数学审校与试做

试做结论：

- 6 题均已完整试做；正确答案为 Q01 A、Q02 A、Q03 A/B/C、Q04“错误，实际左移 π/9”、Q05“原式方向错，可取 `y = -2 sin 2x`”、Q06 A。
- 数学口径复核通过：振幅为 `|A|`；在本题组 `ω > 0` 的题设和候选式下，周期满足 `T = 2π / ω`，所以 `ω = 2π / T`；水平平移量统一写作 `h = -φ / ω`，再转译为“向左/向右平移”的课堂语言；`A < 0` 表示关于 x 轴翻折，不改变振幅大小。
- Q01 能区分周期和 `ω` 换算；Q02、Q04 分别检查“`φ` 不是平移距离”和平移方向；Q03、Q05 检查 `A < 0` 翻折；Q06 要求同时用振幅、周期和 `x = 0` 关键点排除干扰项。
- 课堂用时判断：选择题和多选题约 3 到 4 分钟，Q04、Q05 允许短句或条目式作答后，整组预计 7 到 8 分钟可完成；若班级书写速度慢，可把 Q05 的解析式说明改为口头讲评。

修改内容：

- 将题干、选项、反馈、评分规则、教师说明和 README 中课堂可见的 `pi`、`omega`、`phi` 统一为 `π`、`ω`、`φ`，与前三轮已审校 Diagnosis 资源的符号口径一致。
- 强化相位平移口径：在 README、metadata、Q02、Q03、Q04 和评分规则中补入 `h = -φ / ω`，避免只写 `φ / ω` 导致方向语言不够清楚。
- 收紧 Q03 多选评分映射：漏选周期读数、漏选 `A` 的负号或误选“`φ = π/2` 就左移 `π/2`”时分别指向对应错因标签。
- 将 Q05 题干从“右侧先下降”改为“函数值在 `x = 0` 右侧先从 0 变小”，降低语言歧义。
- 将教师说明的 8 分钟流程调整为 1 到 7 分钟作答、7 到 8 分钟看板回讲，明确 Q04、Q05 可用短句或条目式回答。

文件一致性检查：

- `metadata.yaml` 与 `item-bank.yaml` 均为 6 题、预计 8 分钟，错因标签保持 5 类一致。
- `item-bank.yaml` 总分为 10 分；`scoring-rubric.md` 的题号、分值、掌握阈值 7.5 分和错因优先级与题组一致。
- `teacher-notes.md` 的讲评路径覆盖周期换算、平移量未除以 `ω`、平移方向错误、`A < 0` 翻折遗漏和多参数干扰，能对应题组标签。
- `README.md` 的诊断链路已同步为“读振幅 -> 读周期 -> 求 `ω` -> 算 `h = -φ / ω` -> 关键点验证”。

剩余风险：

- 尚未进行真实小组限时试做；若学生代数书写较慢，Q04 或 Q05 可能需要教师口头收束，暂不建议直接进入 `classroom_trial`。
- 后续 Diagnosis 播放器接入时，Q04、Q05 的错因标签仍需要教师端人工勾选或半自动判定，不能只靠字符串匹配。
- 本资源默认候选式和题设均采用 `ω > 0`；若后续扩展到 `ω < 0` 的等价表达，需要另设题组或在说明中单独处理。

下一状态建议：

- 建议进入 `math_review`：本轮数学表达、题组结构和文件一致性已达到数学审校入口要求。
- 暂不建议进入 `classroom_trial`：需至少一次教师试读或小组限时试做，并确认简答/错误分析题的错因标签采集方式。
