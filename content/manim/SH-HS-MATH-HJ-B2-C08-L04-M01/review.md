# 审核记录：投影有向长度导入动画

资源 ID：`SH-HS-MATH-HJ-B2-C08-L04-M01`

当前状态：`draft`
当前阶段：视频产物已生成；顶层状态仍保持 `draft`，平台卡片已补齐 `video_ready` 入口

## 2026-05-11 单资源自检结论

- 数学主线已从占位稿推进为可审看的 Manim 草稿：固定向量 `b` 的正方向，向量 `a` 的终点向 `b` 所在直线作垂线，投影点 `H` 决定有向投影长度。
- 锐角、直角、钝角三段已分别呈现：`投影长度 = |a|cosθ > 0`、`投影长度 = 0`、`投影长度 = |a|cosθ < 0`。
- 数量积只做预告：画面末尾提示“下一课：用有向投影理解数量积”，没有展示下一课的定义式、运算律或应用题。
- 画面表达已统一为中文课堂可接受符号：`θ`、`cosθ`、`投影长度`、正方向、反方向、垂线、投影点 `H`。
- 内容为原创 Manim 分镜和场景脚本，没有使用外部图片、音频、教材正文、官方课件或商业平台资源。

## 渲染记录

- Manim：`Manim Community v0.20.1 via uvx`。
- 语法检查：`python3 -m py_compile scene.py` 已通过。
- 低质量预览：`uvx manim -ql --media_dir dist scene.py DirectedProjectionLengthScene` 已通过。
- 1080p 渲染：`uvx manim -r 1920,1080 --fps 30 --format=mp4 --media_dir dist scene.py DirectedProjectionLengthScene` 已通过。
- 最终 mp4：`dist/final/SH-HS-MATH-HJ-B2-C08-L04-M01.mp4`，1920×1080，30fps，36.8 秒。
- WebM 转码：`dist/final/SH-HS-MATH-HJ-B2-C08-L04-M01.webm`，1920×1080，30fps，36.8 秒。
- Poster：`dist/final/SH-HS-MATH-HJ-B2-C08-L04-M01-poster.png`，1920×1080，取自 34 秒最终衔接画面。

## 2026-05-19 教师观看门槛补充复核

本次复核只完善教师观看确认记录、暂停点、口播衔接和平台入口说明；未重新渲染，也未修改 `scene.py` 或 `dist/final/` 视频产物。

已检查：

- `scene.py` 时间轴与 36.8 秒视频产物一致；本片无音轨，适合教师现场口播。
- `mp4` / `webm` 均为 1920×1080、30fps、36.8 秒，poster 为 1920×1080 PNG。
- 抽帧 contact sheet 显示锐角、直角、钝角和末尾衔接画面均能读到关键元素。
- 平台入口仍为 `platform_card.availability: video_ready`，这只表示可预览视频，不代表进入课堂试用或发布链路。

教师观看门槛：

- 播放前确认两处必暂停点：约 `00:09-00:11` 垂线与 `H` 稳定后，约 `00:27-00:29` 钝角读数稳定后。
- 口播必须补明“有向投影长度”不是普通非负线段长。
- 末尾只作为 L05 数量积的轻量预告，教师不得在本片结束时提前写出完整数量积定义式或运算律。
- 真实课堂使用前仍需教师确认 36.8 秒播放节奏、暂停追问和从视频切换到 `SH-HS-MATH-HJ-B2-C08-L04-A01` 的操作是否顺畅。

## 2026-05-19 教师观看审核结论

结论：可进入内部“教师观看确认队列”，队列标记对应 `teacher_watch_ready`；这只是请教师完成观看确认，不提升正式资源状态。

审核依据：

- `platform_card.availability: video_ready` 只表示平台资源卡已有 `webm` / `mp4` / poster 预览入口，可供教师观看；它不表示已通过课堂试用、候选发布或正式发布。
- 视频产物复核为 1920×1080、30fps、36.8 秒，节奏适合短导入；两处关键 pause-point 应落在画面文字稳定后：约 `00:09-00:11` 垂线、`H` 与有向线段稳定后，约 `00:27-00:29` 钝角读数稳定后。
- 本片无内置音轨，教师口播必须补出“有向投影长度”语言，尤其要说明画面中的“投影长度”读的是有方向符号的长度，不是普通非负线段长。
- 画面把 `H` 在正方向、与 `O` 重合、在反方向三种位置对应到 `|a|cosθ` 的正、零、负，符合 L04“向量的投影”的导入边界。
- 数量积只以“下一课：用它理解数量积”出现，没有把数量积定义式、运算律或应用作为 L04 正式结论。

流转边界：本次只允许进入教师观看确认队列；不得据此标记为 `classroom_trial`、`release_candidate` 或 `published`，顶层 `metadata.status` 继续保持 `draft`。

## 仍需人工观看确认

- 教师需确认“有向投影长度”这一说法是否与本校课堂语言一致；若习惯说“向量在某方向上的投影”，应在讲解时补一句这里读的是有向长度。
- 第 8 章课时边界仍需纸质教材或已登录 dolearning/上海数字教学平台终核，本资源不应被视作发布态。
- 后续数学审校需确认末尾数量积预告不会抢占 `SH-HS-MATH-HJ-B2-C08-L05` 的完整数量积教学。

## 下一内部流转建议

- 顶层 `metadata.status` 保持 `draft`。
- 可进入内部“教师观看确认队列”，用于完成教师观看、口播和暂停点确认。
- 已在 metadata 中补齐 `render_plan.phase: rendered`、`files.output_mp4`、`files.output_webm`、`files.poster` 和 `platform_card.availability: video_ready`，平台可识别稳定视频入口。
- 不建议进入课堂试用、候选发布或正式发布链路；这些正式状态只能由总控在后续审校后决定。

## 数学审校关注点

- “投影长度”是否需要在教师口播中明确补成“有向投影长度”，避免学生只理解为非负线段长。
- 钝角时 `H` 落在反方向、`cosθ < 0`、投影长度为负三者是否足够直观。
- 直角时投影点与 `O` 重合的瞬间是否足够清楚，避免误读成线段消失导致无法定义。

## 教学审校关注点

- 约半分钟到四十秒的节奏是否适合放在 `SH-HS-MATH-HJ-B2-C08-L04-A01` Applet 之前。
- 在直角或钝角状态是否需要教师暂停，让学生先判断投影长度符号。
- 末尾数量积预告是否足够轻，不抢占下一课。

## 视觉与渲染关注点

- 教室大屏上 `a`、`b`、垂线、投影点 `H` 和有向投影线段是否区分明显。
- 中文字体、`θ`、`cosθ` 和有向投影长度表达在本地渲染和平台视频中是否无替换异常。
- poster 应选择钝角读数或最终衔接画面，不应停在淡入空白帧。
