# 审核记录：投影有向长度导入动画

资源 ID：`SH-HS-MATH-HJ-B2-C08-L04-M01`

当前状态：`draft`
当前阶段：`metadata_ready`

## Scaffold 自检

- 已对齐课时 `SH-HS-MATH-HJ-B2-C08-L04`「向量的投影」。
- 已生成 Manim 必备文件：`metadata.yaml`、`README.md`、`storyboard.md`、`scene.py`、`review.md`。
- 已将分镜范围限定为 30 到 45 秒导入：作垂线、读有向投影、解释 `|a| cos theta` 的正负。
- 当前场景只用于占位，不代表动画已经完成。
- 本轮未渲染 `mp4`、`webm` 或 poster，metadata 中未声明不存在的视频产物。
- 本轮未处理 8.3 暂缓资源。

## 数学审校关注点

- “投影长度”表述是否需要统一为“有向投影长度”，避免学生只理解为非负线段长。
- 向量 `a` 投影到向量 `b` 方向时，`theta` 的取值范围和正负号解释是否清楚。
- 钝角时 `|a| cos theta < 0` 的视觉位置是否足够直观。
- 数量积公式只作为衔接出现，避免抢到下一课时完整推导。

## 教学审校关注点

- 是否适合作为 `SH-HS-MATH-HJ-B2-C08-L04-A01` 前的短导入。
- 暂停问题是否能逼学生区分“普通长度”和“有向读数”。
- 30 到 45 秒时长是否足以建立直觉，又不挤占交互探索时间。

## 待补充

- 完整 Manim 动画节奏、几何对象位置和符号标注。
- 本地语法检查和低质量预览。
- 渲染 `mp4`、`webm`、poster 后再升级 `render_plan.phase` 与 `platform_card.availability`。
- 数学审校与课堂播放验证。
